<ul>
  <% @games.each do |game| %>
    <li>
      <%= link_to "Game#{game.id}", game_path(game) %>, создал
      <%= game.users.first.email %>
    </li>
  <% end %>
</ul>

<a id="test" href="#">TEST</a>
